# .readthedocs.yml

build:
    image: latest

requirements_file: 
    requirements.txt

python:
    version: 3.6
    setup_py_install: true
    pip_install: false
    use_system_site_packages: true
    extra_requirements: 
        [any_reg, dateutil, linearmodels]
        